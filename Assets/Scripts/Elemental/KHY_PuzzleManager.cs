using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// ElementManager에서 원소와 상호작용한 결과로 PuzzleManager에 신호를 보낼 예정

public class KHY_PuzzleManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
